# Requirements using only pre-compiled wheels (no compilation needed)
setuptools>=70.0.0
wheel>=0.42.0

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Basic dependencies
requests==2.31.0
python-dotenv==1.0.0
pydantic==2.5.0

# Database - using binary packages only
psycopg2-binary==2.9.9
SQLAlchemy==2.0.23

# AI - using versions with pre-compiled wheels
openai==1.3.0

# Use specific numpy version with pre-compiled wheels
numpy==1.24.4
